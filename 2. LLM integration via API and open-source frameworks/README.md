# 🧠 Лабораторна робота №2  
**Дисципліна:** Нейромережеві технології і системи  
**Тема:** Інтеграція LLM через API та open-source фреймворки  
**Варіант:** №1 — LM Studio + локальний OpenAI-сумісний сервер  

---

## 🎯 Мета роботи
Ознайомитися з принципами інтеграції великих мовних моделей (LLM) у прикладні системи;  
навчитися працювати з **локальними інструментами (LM Studio)** через OpenAI-compatible API;  
дослідити вплив **system prompt**, **temperature**, **max tokens** на поведінку моделі.

---

## ⚙️ Реалізація

### 🧩 Основні етапи:
1. **Встановлення LM Studio**  
   - Завантажено з [офіційного сайту](https://lmstudio.ai).  
   - Обрана модель: **`google/gemma-3n-e4b`** (≈6.9 B параметрів).  

2. **Запуск локального сервера**  
   - Активовано режим **OpenAI-compatible API**.  
   - Перевірено доступність за адресою `http://localhost:1234/v1`.

3. **Реалізація CLI-програми**  
   - Використано **Python 3.11** і бібліотеку **OpenAI** для запитів.  
   - Підтримується введення запитань, system prompt, temperature, max tokens.  
   - Відповіді відображаються в консолі з форматуванням через `rich`.  
   - Усі діалоги зберігаються у директорію `logs/` як `.md`-файли.

4. **Параметри запуску**
   ```bash
   python lm_cli.py --system "Ти AI-асистент, відповідай українською."                     --temperature 0.4                     --max-tokens 128                     --model local-model
   ```

---

## 📁 Структура проєкту
```
project/
│
├── lm_cli.py
├── logs/
│   └── dialog_YYYYMMDD_HHMMSS.md
├── .env
├── .gitignore
└── README.md
```

---

## 🧠 Основні файли

### `lm_cli.py`
Реалізує консольну взаємодію з локальним LLM-сервером LM Studio.  
Ключові бібліотеки: `argparse`, `dotenv`, `rich`, `openai`.

### `.env`
Містить параметри середовища:
```bash
OPENAI_BASE_URL=http://localhost:1234/v1
OPENAI_API_KEY=lm-studio
```

### `.gitignore`
Ігнорує середовище, логи, кеш та модельні файли.

---

## 💬 Приклад виконання

```
🔹 Підключення до LM Studio...
URL: http://localhost:1234/v1
Модель: local-model

💬 Введи свій запит (напиши 'exit' щоб вийти')

🧠 Ти: Привіт, розкажи про квантовий комп’ютер
🤖 Модель: Квантовий комп’ютер — це пристрій, який обчислює, використовуючи квантові біти...
```

🗂 **Діалог збережено:** `logs/dialog_20251101_143000.md`

---

## 🧮 Контроль параметрів генерації
| Параметр | Діапазон | Вплив |
|-----------|-----------|--------|
| `temperature` | 0.0 – 2.0 | регулює креативність (низьке = логічно, високе = креативно) |
| `max_tokens` | > 0 | обмежує довжину відповіді |
| `system prompt` | будь-який текст | визначає стиль та поведінку моделі |

---

## 📈 Результати роботи
- Програма успішно підключається до локального API.  
- Модель коректно генерує відповіді українською мовою.  
- Реалізовано логування всіх діалогів.  
- Перевірено наявність цензури — модель не надає небезпечного контенту.  

---

## 💬 Висновки
У результаті роботи встановлено та налаштовано **LM Studio**, завантажено модель із Hugging Face, створено CLI-програму для взаємодії через локальний OpenAI-сумісний API.  
Досліджено вплив параметрів `system prompt`, `temperature`, `max_tokens` на поведінку моделі та перевірено її безпеку.  
Отриманий досвід заклав основу для розробки інтелектуальних застосунків із локальною обробкою мовних запитів.

---

## 👩‍💻 Автор
**Оліярник Софія**  
Група ОІ-45  
Спеціальність 122 — *Комп’ютерні науки (Обчислювальний інтелект та смарт-системи)*  
Національний університет «Львівська політехніка»